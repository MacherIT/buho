<%= bootstrap_form_for(@red) do |f| %>

  <%= f.alert_message "Han ocurrido los siguientes problemas:" %>

  <% unless @red.persisted? %>
    <%= f.hidden_field :cliente_id, value: @cliente.id %>
  <% else %>
    <h4><%= @red.cliente.nombre %></h4>
  <% end %>

  <%= f.select :tipo, REDES_TIPOS.map { |k, v| [k.capitalize, v] }, required: true, label: "Red"%>
  <div id="p_red_fb">
    <%= f.text_field :token %>
  </div>
  <div id="p_red_ig" style="display: none;">
    <%= f.text_field :user %>
    <%= f.text_field :pass, type: 'password' %>
  </div>
  <%= f.text_field :nombre %>
  <%= f.text_field :nombre_display %>

  <%= f.primary %>
  <%= link_to "Atrás", redes_path, class: "btn btn-link" %>

<% end %>
