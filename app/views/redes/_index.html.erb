
<table class="table table-striped">
  <thead>
    <tr>
      <th>Tipo</th>
      <th>Token</th>
      <th>Nombre</th>
      <th>Nombre display</th>
      <th>Cliente</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @redes.each do |red| %>
      <tr>
        <td><%= red.tipo_cast %></td>
        <td><%= red.token.blank?? '---' : red.token[0..10] + '...' %></td>
        <td><%= red.nombre %></td>
        <td><%= red.nombre_display %></td>
        <td><%= red.cliente.nombre %></td>
        <td>
          <div class="btn-group">
            <%= link_to red, class: "btn btn-xs btn-default" do %>
              <%= fa_icon("eye", text: "Ver") %>
            <% end %>
            <%= link_to edit_red_path(red), class: "btn btn-xs btn-default" do %>
              <%= fa_icon("pencil", text: "Editar") %>
            <% end %>
            <%= link_to red, class: "btn btn-xs btn-danger", method: :delete, data: { confirm: "¿Seguro de eliminar este red?" } do %>
              <%= fa_icon("trash-o", text: "Eliminar") %>
            <% end %>
          </div>
      </tr>
    <% end %>
  </tbody>
</table>
